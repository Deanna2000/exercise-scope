//Conjunction Function

//Create the function with 2 paraments for 2 words to be passed in
const conjunction = function (firstWord, secondWord) {
    const conjoinedWord = `${firstWord} ${secondWord}` //Use interpolation to join the words
    console.log(conjoinedWord)
}

conjunction("Master", "Card") //Invoke the function
